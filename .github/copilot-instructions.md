# Copilot Instructions

Use `AGENTS.md` and `CONTEXT.md` as the canonical project instructions.

When suggesting code:

- preserve chronological backtesting semantics;
- avoid look-ahead bias;
- keep portfolio accounting signs correct;
- prefer existing project module boundaries;
- do not reference or generate code that assumes raw market data is committed.
