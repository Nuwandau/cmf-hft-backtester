# Claude Instructions

Read `AGENTS.md` first, then `CONTEXT.md`. These files contain the canonical
project workflow, quant invariants, commands, and modeling assumptions.

Do not duplicate instructions here. Keep this file as a pointer so that all
coding agents share the same source of truth.
